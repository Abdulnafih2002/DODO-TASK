# Remediated, pinned dependencies for the hardened build.
# The starter pinned Flask 0.12.2 / Werkzeug 0.14.1 / PyYAML 5.1 / requests 2.19.1
# — all carry known CVEs (see Task 4 report). These are the patched successors.
Flask==3.0.3
Werkzeug==3.0.6
PyYAML==6.0.2
requests==2.32.3
Jinja2==3.1.6
gunicorn==23.0.0
