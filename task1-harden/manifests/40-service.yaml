apiVersion: v1
kind: Service
metadata:
  name: ledger-api
  namespace: payments
  labels:
    app: ledger-api
spec:
  type: ClusterIP
  selector:
    app: ledger-api
  ports:
    - name: http
      port: 8080
      targetPort: http
